# Dev Flow Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Codify the workspace-based development workflow for working on the `octothorpes` core package alongside the SvelteKit site.

**Architecture:** Add npm scripts and document the workflow. No code changes to business logic.

**Tech Stack:** npm workspaces, Vitest

---

### Task 1: Add core test script

**Files:**
- Modify: `package.json` (root)

- [ ] **Step 1: Add `test:core` script to root `package.json`**

Add to the `scripts` section:

```json
"test:core": "npx vitest run src/tests/exports.test.js src/tests/core.test.js src/tests/api.test.js src/tests/badge.test.js src/tests/harmonizer.test.js src/tests/converters.test.js src/tests/publish.test.js src/tests/blobject.test.js src/tests/indexer.test.js"
```

- [ ] **Step 2: Verify it works**

Run: `npm run test:core`
Expected: Runs only the core-relevant test subset. All pass.

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "chore: add test:core script for core package test subset"
```
